import { injectable } from "inversify";
import { Body } from "matter-js";

@injectable()
class Collision {
  addBody(body: Body) {
    
  }

  removeBody(body: Body) {

  }
}

export default MatterJsCollision;